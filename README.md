#jaildingtalk doc

1. 安装`OpenDev`编写代码     
2. 下载砸壳 ipa 解决压缩    
3. `yololib`执行文件放在`xxx.app`同级目录中，cd 到此目录文件夹，执行命令 `./yololib xxx.app/xxx yourdylib.dylib`     
4. 利用 ios-app-resign 重新签名。**注意：这里不能签名 watch 等，因此重签名之前需要删除`xxx.app`中的这些文件**


